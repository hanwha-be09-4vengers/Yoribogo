# MongoDB의 공식 Alpine 기반 이미지를 사용
FROM mongo:latest

# 환경 변수 설정
ARG MONGO_INITDB_ROOT_USERNAME
ARG MONGO_INITDB_ROOT_PASSWORD
ARG MONGO_INITDB_DATABASE

# Dockerfile에서 직접 설정할 경우 기본값을 제공
ENV MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME:-default_username}
ENV MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD:-default_password}
ENV MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE:-default_database}
